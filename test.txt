import system
import file
import ui

intern_class String
{
    intern_function String();
    intern_function append();
    intern_function toString();
    intern_function getBytes();
}

var array = [4];
array.set(4, 1);

var intern_string = new String("Hello World!");
intern_string.append("Hah");
system.print(intern_string.toString());
intern_string.getBytes();

var program = file.read("test.txt");
system.print("Current Program: ");
system.print("");
system.print(program);
system.print("");

var drawHandler = function drawHandler(canvas, width, height)
{
    system.print("Width: " + width + ", Height: " + height);
    canvas.drawText("Hello World!", 0, 0);
    canvas.drawImage(image, 0, 0, width, height);
};

var image = ui.createImageFromFile("testImage.jpg");

var window = ui.createWindow();
window.setVisible(true);
window.setSize(1000, 600);
window.setName("My Window");
window.setDrawHandler(drawHandler);
window.centerOnScreen();

return 0;